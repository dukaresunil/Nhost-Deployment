import { NhostClient } from '@nhost/react';

const nhost = new NhostClient({
  subdomain: 'hrwnxbkjidebhmowwfrc', // Replace with your Nhost subdomain
  region: 'ap-south-1', // Replace with your Nhost region
});

export default nhost;
